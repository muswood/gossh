<!-- owner: muswood | Email: mumu920@outlook.com -->
# GoSSH 使用说明

GoSSH 是一个面向单机运维用户的 Windows 桌面工作台，将 SSH / Telnet / 原始 TCP / 串口终端、SFTP 文件管理、端口转发、连接管理，以及内置的可审计 AI Agent 集成在一个 Wails 应用中。

本说明基于当前源码实现编写，仅将程序中实际存在的功能列为可用；AI 提示词中的运维规则不等同于程序级拦截。

## 目录

1. [安装与启动](#1-安装与启动)
2. [界面总览](#2-界面总览)
3. [创建与导入连接](#3-创建与导入连接)
4. [SSH / Telnet / Raw TCP / 串口会话](#4-ssh--telnet--raw-tcp--串口会话)
5. [终端操作与可见会话日志](#5-终端操作与可见会话日志)
6. [SFTP 文件管理](#6-sftp-文件管理)
7. [端口转发](#7-端口转发)
8. [AI 助手与 Agent](#8-ai-助手与-agent)
9. [本地知识库、MCP 与 Skill](#9-本地知识库mcp-与-skill)
10. [安全配置](#10-安全配置)
11. [本地数据、加密与备份](#11-本地数据加密与备份)
12. [常见问题与限制](#12-常见问题与限制)

---

## 1. 安装与启动

### 系统要求

- Windows 10/11（amd64）。
- Microsoft Edge WebView2 Runtime（推荐最新的 Evergreen 版本）。
- 无需安装 Go、Node.js 或 Wails；这些仅用于源码构建。

### 启动

- 直接运行 `build/bin/gossh.exe`。
- 首次启动会在用户目录创建数据目录 `~/.gossh`（即 `C:\Users\<用户名>\.gossh`），用于保存配置、加密密钥、知识库、Agent 状态和终端日志。

### 构建（仅开发者）

```bash
GOCACHE=/tmp/gossh-go-build-cache wails build -platform windows/amd64
```

产物位于 `build/bin/gossh.exe`。

---

## 2. 界面总览

主窗口由四部分组成：

| 区域 | 位置 | 作用 |
|---|---|---|
| 连接树 | 左侧 | 搜索、分组、保存的连接列表，以及新建连接/分组入口 |
| 标签栏 | 顶部 | 管理打开的标签页，支持分屏 |
| 内容区 | 中部 | 终端、欢迎页、设置页、SFTP、端口转发、AI 助手 |
| 状态栏 | 底部 | 侧边栏开关、显示/隐藏 SFTP、显示/隐藏 AI、主题、设置 |

### 欢迎页

首次启动进入欢迎页，提供四张卡片：

- **SSH 连接**：打开新建连接弹窗（默认 SSH 类型）。
- **串口连接**：打开新建连接弹窗（默认串口类型）。
- **AI 助手**：打开一个独立的 AI 助手标签页。
- **应用设置**：打开设置页。

### 连接树

- **搜索**：按名称或主机地址过滤连接。
- **分组**：点击分组名展开/折叠；悬停后可用图标重命名、删除、设置颜色。
- **连接项**：点击建立会话；悬停后可用图标收藏、编辑、删除。
- **底部按钮**：`分组` 新建分组，`新建连接` 打开连接弹窗。

### 标签与分屏

- 可同时打开多个 SSH/TCP/串口/AI 标签。
- 支持分屏显示多个终端（在标签栏控制，垂直或水平排列）。
- 关闭标签会同步清理对应的会话资源。

### 会话恢复

应用重启后会自动尝试恢复最近使用过的最多 8 个 SSH 连接；恢复失败的标签会自动移除，不影响其他会话。

---

## 3. 创建与导入连接

点击「新建连接」进入连接弹窗，顶部可切换连接类型：**SSH**、**串口**、**Telnet**、**Raw TCP**。

### SSH 连接字段

| 字段 | 说明 |
|---|---|
| 连接名称 | 可选，缺省使用主机地址 |
| 主机地址 | 服务器 IP 或域名 |
| 端口 | 默认 22 |
| 用户名 | 默认 root |
| 认证方式 | 密码 / 密钥 / SSH Agent |
| 编码 | UTF-8 / GBK / GB2312 |
| 心跳间隔 | 秒，默认 30，0 表示不发送 keepalive |
| 终端配色 | 该连接单独使用的终端主题，留空使用全局主题 |
| 跳板机 | 支持多跳，逗号分隔，例如 `root@jump-a:22,jump-b:22` |
| 网络代理 | 不使用 / HTTP CONNECT / SOCKS5 / ProxyCommand |
| 登录后执行命令 | 连接成功后自动发送，例如 `sudo -i` |

### 认证方式

- **密码**：填写密码。
- **密钥**：填写私钥内容或私钥文件路径（支持 `~/.ssh/id_ed25519`），可选 OpenSSH 用户证书路径和私钥口令。
- **SSH Agent**：使用系统 SSH Agent（含 FIDO2 / PKCS#11）。需先通过 `ssh-add` 加载密钥，且设置了 `SSH_AUTH_SOCK` 环境变量。

### 首次连接的主机密钥信任

连接从未见过或密钥已变化的主机时，会弹出主机指纹确认。只有在你确认指纹匹配后才会写入 `known_hosts` 并继续；指纹不匹配会直接拒绝。

### 键盘交互认证（MFA）

服务器要求二次验证（例如 TOTP、密码 + 验证码）时，会弹出交互认证窗口，逐项填写回答后提交，或选择取消。

### 导入

在「设置 → 通用 → OpenSSH 集成」中：

- **导入 SSH Config**：从 OpenSSH 配置文件导入具体 Host 条目（忽略仅通配符条目），并自动识别 IdentityFile、ProxyJump、ProxyCommand、端口转发等。
- **导入 known_hosts**：将选定的 known_hosts 文件合并到标准信任库，不覆盖已有主机密钥。

在「设置 → 通用 → 数据管理」中：

- **导出配置** / **导入配置**：导出或导入全部连接、分组和用户 Skill（JSON，敏感字段加密处理）。

---

## 4. SSH / Telnet / Raw TCP / 串口会话

| 类型 | 交互终端 | AI 终端命令 | SFTP / 端口转发 |
|---|---|---|---|
| SSH | 支持 PTY | 支持；使用远端完成标记，提示符为回退 | 支持 |
| Telnet | 支持 | 支持；必须由设备返回可识别提示符 | 不支持 |
| Raw TCP | 支持 | 不支持作为可靠的自动执行目标 | 不支持 |
| 串口 | 支持 | 不支持作为 Agent 终端执行目标 | 不支持 |

- **SSH**：点击连接树中的 SSH 连接项即可建立交互式 PTY 终端。
- **Telnet / Raw TCP**：选择对应协议类型，填写主机和端口后连接。Telnet 会处理常见协商选项（Echo、SGA、终端类型、窗口大小 NAWS）。
- **串口**：选择端口名、波特率、数据位、停止位、校验位，可启用断线自动重连。串口终端为独立面板，不写入 SSH 会话日志。

Telnet 网络设备应关闭分页或将分页长度设为足够大，并确保执行后回到稳定提示符，例如 `Router#`、`<HUAWEI>` 或设备自定义提示符；否则 Agent 无法安全确认命令结束。

连接失败时会删除标签、返回欢迎页并显示真实错误；失效认证、网络失败或未知主机密钥不会被报告为已连接。

---

## 5. 终端操作与可见会话日志

终端基于 xterm.js，支持：

- 文本与二进制输入输出；二进制写入通过 Base64 通道，保证 X/Y/ZMODEM 文件传输字节不被破坏。
- 复制、粘贴、搜索、保存当前终端内容到本地文件。
- 主题、背景图、字体、字号、光标样式、回滚行数等显示设置（在「设置 → 终端」中调整）。
- Shell Integration 命令块与错误块解析，可将选中文本或终端上下文交给 AI 分析。
- ZMODEM / XMODEM / YMODEM 文件传输，显示传输状态、进度、错误与取消。

### 可见会话日志

每个已连接的 SSH 会话会在本机记录终端可见文本：

```text
~/.gossh/logs/YYYY-MM-DD/<session-id>.log
```

- 目录权限 `0700`，日志文件权限 `0600`，追加写入并每次 `Sync()`。
- 仅记录协议解析后实际交给 xterm 的可见内容；ZMODEM/XMODEM/YMODEM 协议帧及 ACK/NAK/EOT/CAN 等控制字节不写入日志。
- 写入前会移除 ANSI 颜色/控制序列和 Shell Integration 元数据。
- TCP、Telnet 和串口会话不写入 SSH 会话日志。

> 日志是「终端可见文本记录」，不是命令审计格式，无法还原颜色、光标定位或覆盖式进度等原始语义。

---

## 6. SFTP 文件管理

在 SSH 会话中，通过状态栏「显示 SFTP」或设置中的开关打开 SFTP 双面板。支持：

- 本地/远端目录浏览、主目录定位。
- 上传、下载，含断点续传、重试和 SHA-256 完成校验。
- 新建目录、读写文件、重命名、删除、递归删除、chmod、符号链接。
- 服务端扩展能力（posix-rename、statvfs、fsync）和磁盘使用量查询。
- 传输进度、重试、续传、完成、失败和取消状态通过事件实时显示。

> SFTP 面板中的手工操作由用户直接发起，不经过 Agent 命令策略或审批；只有 AI Agent 的 `sftp_list_dir` / `sftp_read_file` 需要审批卡。

---

## 7. 端口转发

基于 SSH 会话支持三种转发：

- **本地转发（local / -L）**：监听本地端口，转发到远端。
- **远程转发（remote / -R）**：监听远端端口，转发到本地。
- **动态转发（dynamic / -D）**：本地 SOCKS5 代理。

- 规则会按连接持久化；同一连接重新建立后自动尝试恢复。
- 停止转发会关闭监听器；启动失败会返回真实错误。

---

## 8. AI 助手与 Agent

### 配置模型

在「设置 → AI 配置」中配置模型连接：

| 字段 | 说明 |
|---|---|
| 模型供应商 | DeepSeek / OpenAI / Claude / 通义千问 / New API / 自定义 |
| 模型名称 | 例如 `deepseek-chat` |
| Embedding 模型 | 例如 `text-embedding-3-small` |
| API Key | 加密保存；留空表示保留已保存的密钥 |
| 自定义 API 地址 | 填写到版本根路径，例如 `https://example.com/v1` |
| API 模式 | Chat Completions；OpenAI 可选 Responses + Tools |
| 最大输出 Token | 1–900000 |
| Agent 最大迭代次数 | 1–50，默认 16，控制单次分析可调用的模型/工具轮数 |
| AI 历史保存天数 | 默认 7 天，0 表示不保存 |

> 注意：Eino Agent 目前不支持 Claude 或 OpenAI `responses` 模式下的工具调用，遇到时会提示切换到 Chat Completions 模式。

### 打开 AI 助手

- 欢迎页「AI 助手」卡片打开独立 AI 标签。
- 在 SSH、Telnet、Raw TCP 会话中，通过状态栏「显示 AI」在右侧打开与该终端关联的 AI 面板；Raw TCP 仅适合聊天与已有输出分析，不能依赖 Agent 自动终端执行。
- 连接历史以已保存连接的 `connectionId` 隔离；不同 SSH、Telnet、Raw TCP 和串口连接不会自动混用历史。无法归属的旧历史不会自动附加到当前连接。

### 普通 Agent 与专有 Agent

- **普通 Agent**（聊天、命令解释、命令生成、一般问答）：以自然语言直接回复，不强制生成报告。
- **专有 Agent**（诊断、终端输出分析、Skill 工作流、角色化任务）：结束前必须生成经过校验的结构化报告（包含标题、摘要、严重性和至少一条证据）。

### 审批流程

Agent 在真正执行以下操作前，都会弹审批卡并等待你确认：

- 远端终端命令（显示完整命令、目的、风险、目标）。
- 远端 SFTP 读取。
- 读取本机 GoSSH 配置摘要（不含密码、私钥、API Key）。
- 读取本机会话日志。
- 读取用户指定的本地文档。
- 互联网搜索和网页读取。
- MCP 外部工具调用。

拒绝后 Agent 不会执行该工具，也不能通过其他工具绕过审批。

### 终端命令执行与完成判定

Agent 会实时接收工具输出，并在确认当前命令结束前不会发送下一条命令。单步最长等待 30 分钟；对于正常但耗时的命令，例如大目录 `du` 扫描，Agent 不应默认加入远端短时间 `timeout`。需要停止时，由用户直接在终端按 `Ctrl-C`。

- **SSH**：程序将已审批命令包装为 `command; rc=$?; printf '\n__GOSSH_DONE_<随机ID>__:%s\n' "$rc"`，持续读取 PTY 输出，检测到唯一完成标记后返回完整输出和退出码。无法取得标记时，才以重新出现的可靠提示符作为回退判定。
- **Telnet**：不能假定设备具有 Shell 或可执行包装命令，因此只发送原命令，并持续读取数据直到设备返回新的可识别提示符。分页提示会被识别为未完成状态，需先处理分页。
- **Raw TCP / 串口**：没有跨设备通用的结束信号，不能据此安全驱动连续 Agent 命令；仍可由用户在终端中交互操作。

工具输出会在任务时间线中流式显示。Agent 不读取终端屏幕截图；其分析依据是本次工具输出，以及在单独审批后读取的本地 SSH 会话日志或其他本地资料。

### 任务恢复与停止

- Agent 任务以「任务」为单位运行，包含步骤、事件、审批和 checkpoint。
- 应用重启后，未完成的任务标记为可恢复；恢复时已完成步骤只作为历史证据，未完成步骤可重放，审批必须重新发起。
- 可随时停止任务；停止会取消关联终端执行。

---

## 9. 本地知识库、MCP 与 Skill

### 本地知识库（RAG）

在「设置 → AI 配置 → 本地知识库」中：

- 添加文档（标题 + 内容）、检索、删除文档。
- 「更新向量索引」增量重建向量索引。
- 向量后端可选「本地 SQLite」或「Qdrant ANN」（配置 Endpoint、Collection 和 API Key）。文档和访问范围始终保留在本地，Qdrant 仅同步向量。
- 检索采用词法 + 向量混合评分，支持匹配词、来源、版本和排名元数据。

启用「启用本地知识库」开关后，Agent 才可使用 `rag_search` 检索本机文档。

### MCP 工具服务

在「设置 → AI 配置 → MCP 工具服务」中：

- 配置 stdio 或 Streamable HTTP 服务，填写服务标识、启动命令/参数（或 HTTP Endpoint）、环境变量（加密保存）、可选认证令牌和工具白名单。
- 工具白名单为空表示暴露全部工具；配置后仅暴露列出的工具。
- 所有 MCP 工具在 Agent 中经审批包装后调用；配置目标 ACL 时必须显式提供已授权目标。
- 模型不能自行选择或启动任意可执行文件，只能使用这里配置的服务。

### Agent Skill

Skill 是可复用的 Agent 模板，只能调用其列出的工具。内置三个：

- `linux-health-check`：Linux 健康检查。
- `service-diagnosis`：服务故障诊断。
- `remote-file-audit`：远端文件审计。

支持：

- 导入 / 导出 / 编辑标准 `SKILL.md`（仅 name + description Front Matter 和 Markdown 正文）。
- 启用 / 禁用、删除、搜索、查看并恢复历史版本。
- Ed25519 签名与可信公钥管理：保存/撤销可信公钥、生成密钥、对编辑器内容签名。

> Skill 的工具白名单只会进一步缩小 Agent 能力，不会绕过审批和命令策略。

---

## 10. 安全配置

在「设置 → 安全」中配置 Agent 命令执行边界：

| 配置 | 默认 | 说明 |
|---|---|---|
| 只读命令无需审批 | 关 | 开启后，明确判定为只读且通过策略检查的终端命令直接执行；写入、删除、未知命令和 Agent SFTP 读取仍需审批 |
| 命令白名单 | 开 | 只允许列表中的只读命令执行 |
| 命令黑名单 | 开 | 黑名单优先级高于白名单 |
| 允许写操作 | 开 | 关闭后拒绝文件写入、服务启停/重启等变更 |
| 允许删除操作 | 关 | 打开后仍需任务授权、首次审批和二次确认 |
| 管理员模式 | 关 | 关闭 Agent 的命令安全拦截，每条命令仍需人工批准，删除仍需二次确认 |

程序级硬拦截始终生效，不受这些开关影响：

- `bash`、`sh`、`zsh`、`python`、`node`、`sudo`、`su`、`tee`、`xargs` 等解释器/提权命令被拦截。
- 重定向、命令替换、子 Shell、`&&`/`||` 等 Shell 控制符被拦截。
- `kubectl`、`helm`、`docker`、`git`、`systemctl`、`find`、`journalctl` 等仅允许受限子命令。

写入操作还需同时满足：任务允许写操作、全局允许写操作、受限写命令、幂等键、四段安全计划（前置检查/快照/验证/回滚）和用户审批。删除操作默认关闭，开启后仍需二次确认。

> Kubernetes 系统命名空间、控制平面组件、云资源和数据库语义目前主要依赖提示词约束，尚未形成完整的资源级硬拦截。

---

## 11. 本地数据、加密与备份

### 数据目录

```text
~/.gossh/
├── gossh.db          # 连接、分组、设置、AI 配置
├── master.key        # 32 字节主密钥（0600）
├── rag.db            # 本地知识库
├── skills/           # 用户 Skill
├── logs/             # SSH 可见会话日志（按日期分目录）
└── agent ...         # Agent checkpoint（位于 gossh.db 内）
```

### 加密

- 敏感字段（SSH 密码、私钥、私钥口令、代理密码、AI API Key、Qdrant API Key、MCP 环境变量与认证 Token）使用 AES-256-GCM 加密，格式为 `v1:<base64>`。
- Agent checkpoint 在写入时对 password、token、secret、authorization、apikey 等字段脱敏。
- `ListConnections` 和诊断接口不会向前端返回密码、私钥或 API Key。

### 备份注意事项

- 备份 `~/.gossh` 时请一并备份 `master.key`，否则加密配置无法解密。
- 配置导出为 JSON；请勿将包含敏感信息的备份放到公网目录或服务可加载目录。
- 设置中的「诊断」页可查看 Go 版本、SSH 算法能力、known_hosts 状态和最近连接事件，便于排查。

---

## 12. 常见问题与限制

### 常见问题

- **连接后终端黑屏 / 无输出**：确认远程主机可达、端口正确、凭据正确；查看是否弹出错误提示；可在「设置 → 诊断」中查看最近连接事件。
- **AI 未配置 / 报错**：确认已配置 Provider、模型和 API Key；若使用 OpenAI 的 Responses 模式，Agent 工具调用需切换到 Chat Completions。
- **Agent 提示超过最大迭代次数**：在「设置 → AI 配置」中提高「Agent 最大迭代次数」（1–50）。
- **SSH Agent 连接失败**：确认 `SSH_AUTH_SOCK` 已设置且已通过 `ssh-add` 加载密钥。
- **Telnet Agent 一直等待**：确认设备已关闭分页，且命令输出后会出现稳定、可识别的提示符。对设备 CLI 不要使用 SSH 的完成标记包装方式。
- **耗时命令仍在运行**：Agent 会持续等待；仅在确定要中止远端进程时，在当前终端输入 `Ctrl-C`。
- **SFTP 按钮不可用**：SFTP 和端口转发只适用于已连接的 SSH 会话，Telnet、Raw TCP 和串口没有这些能力。

### 当前限制

- 本地文档读取仅依赖逐次审批，没有可配置目录白名单或文件类型策略。
- 网页工具未阻断 DNS/IP 私网地址，内网 SSRF 防护仍需后续补充。
- 断开后的历史会话日志没有独立的后端索引接口，Agent 只能通过日期 + session ID 分页读取本地文件。
- 运维人设中的 Kubernetes 控制面、云资源、数据库规则大部分是提示词，不是完整资源语义强制策略。
- 普通 UI 的直接终端和 SFTP 操作由用户发起，不经过 Agent 命令策略或审批。
- Raw TCP 与串口没有通用、可靠的命令完成信号，不能用于 Agent 自动化连续执行；Telnet 的判定依赖设备提示符和分页行为。
- 无多用户、集中审计、远端策略下发、审批人分离或工单集成。
- 真实 SSH、Qdrant、AI Provider、MCP 服务的行为取决于外部服务、网络和凭据，需在目标环境验证。
